version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: registry.digitalocean.com/clutchbids-registry/scraper-openlane:latest
    container_name: scraper-openlane